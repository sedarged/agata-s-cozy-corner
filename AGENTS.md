# Agents

Start with **[`CLAUDE.md`](./CLAUDE.md)** — it holds the current working state, the active
roadmap, build/verify commands, and conventions. Detailed docs are in [`docs/`](./docs/README.md).

The Lovable Vite wrapper has been **ejected** (`vite.config.ts` now uses native TanStack Start +
Nitro plugins). Supabase has been removed in favour of a local-first approach with a path to
SQLite on the VPS. The branch is no longer synced to Lovable — normal git workflow applies.
