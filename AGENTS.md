# Agents

Start with **[`CLAUDE.md`](./CLAUDE.md)** — it holds the current working state, the active
roadmap, build/verify commands, and conventions. Detailed docs are in [`docs/`](./docs/README.md).

The project is **exiting its Lovable relationship** and moving to a fully self-hosted setup on the
owner's VPS (see [`docs/exit-lovable-plan.md`](./docs/exit-lovable-plan.md)). Until the Vite wrapper
is ejected (Phase 1) and the project is disconnected from Lovable on the owner's side, the connected
branch may still sync back to Lovable — so **avoid force-pushing / rewriting pushed history** for now,
and keep the branch in a working state.
