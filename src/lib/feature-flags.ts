// Dev-time feature flags. Toggle here to re-enable.
// Login UI stays hidden for now (private app). Auth is handled at the network
// level (Caddy/Tailscale on VPS). Gigi uses OPENAI_API_KEY or LOVABLE_API_KEY.
export const SHOW_AUTH_UI = false;
